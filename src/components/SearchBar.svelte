<script>

</script>

<style>
  button {
    float: right;
  }
  input {
    width: 100%;
  }
</style>

<!-- A search bar that you can write within any text with autocomplete capability
which returns the objects from entries.city -->
<form class="border p-3 mb-2 ">
  <!-- onSubmit={handleSubmit} -->
  <button>Search</button>
  <input type="text" name="city" placeholder="Where to?" />
  <!-- onInput={handleInput} onChange={handleInput} value={selectedPrice} {selectedPrice}-->
</form>
